\documentclass[12pt,a4paper]{scrartcl}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\usepackage{times}
\usepackage{algorithm}
\usepackage{xspace}
\usepackage{algpseudocode}
\usepackage{amsmath}
\newcommand{\Eop}[3][\!]{#2#1\to#1#3}
\newcommand{\Assign}[2]{\mbox{$#1\leftarrow #2$}}
\newcommand{\Size}[1]{|#1|}
\newcommand{\Upto}[0]{\textbf{upto}\xspace}
\newcommand{\Subchar}[2]{#1[#2]}
\newcommand{\Substring}[3]{#1[#2\ldots#3]}
\newcommand{\Rtab}[1][j_{0}]{R_{#1}}
\newcommand{\EDcol}[1]{E_{\mathsf{col}}^{#1}}
\newcommand{\Rcol}[1]{\Rtab^{#1}}
\newcommand{\Deletion}[2][\!]{\Eop[#1]{#2}{\varepsilon}}
\newcommand{\Insertion}[2][\!]{\Eop[#1]{\varepsilon}{#2}}
\newcommand{\Replacement}[3][\!]{\Eop[#1]{#2}{#3}}
\newcommand{\Substitution}[3][\!]{\Eop[#1]{#2}{#3}}
\newcommand{\Crosspoints}[0]{C}
\newcommand{\EvalCPname}[0]{\textit{evaluatecrosspoints}}
\newcommand{\Rounddown}[1]{\left\lfloor#1\right\rfloor}
\newcommand{\Evalallcolsname}[0]{\mathit{evaluateallcolumns}}
\newcommand{\Evalallcols}[3]{\Evalallcolsname(E,R,#1,#2,#3)}
\newcommand{\EvalCP}[4]{\EvalCPname(\Crosspoints,#1,#2,#3,#4)}

\begin{document}
%%%BEGIN{frame}
\begin{algorithm}
\caption{Computation of the first and the next distance columns}
\label{FirstNextEDtabcolumn}
\begin{algorithmic}[1]
\Function{\textit{firstEDtabcolumn}}{$E,u$}%ignore
  \State \Assign{E(0)}{0}
  \For{\Assign{i}{1} \Upto \(\Size{u}\)} %
               \Assign{E(i)}{E(i-1)+\delta(\Eop{\Subchar{u}{i}}{\varepsilon})}
  \EndFor
\EndFunction
\Function{\textit{nextEDtabcolumn}}{$E,E^{\prime},u,b$}%ignore
  \State \Assign{E(0)}{E'(0)+\delta(\Eop{\varepsilon}{b})}
  \For{\Assign{i}{1} \Upto \(\Size{u}\)} %
     \Assign{E(i)}{\min\left\{\begin{array}{@{}l@{}}
                 E(i-1)+\delta(\Eop{\Subchar{u}{i}}{\varepsilon})\\
                 E'(i)+\delta(\Eop{\varepsilon}{b})\\
                 E'(i-1)+\delta(\Eop{\Subchar{u}{i}}{b})\end{array}\right\}}
  \EndFor
\EndFunction
\Function{\textit{evaluateallEDtabcolumns}}{$E,u,v$}%ignore
\State \textbf{allocate vector} \(E'\) of length \(\Size{u}+1\)
\State \(\mathit{firstEDtabcolumn}(E,u)\)
\For{\Assign{j}{1} \Upto \(\Size{v}\)} %
  \Assign{E'}{E}; \(\mathit{nextEDtabcolumn}(E,E',u,\Subchar{v}{j})\)
\EndFor
\EndFunction
\end{algorithmic}
\end{algorithm}
%%%END{frame}

\begin{algorithm}
\caption{The recurrence for table \(\Rtab\). The values depend on
column \(\EDcol{j-1}\) and \(\EDcol{j}\). Line 3 handles the
case where the insertion edge is minimizing. Line 4 handles the case where the
replacement edge is minimizing. Line 5 handles the case where the deletion
edge is minimizing. If \(j>j_{0}\), then at least one of the three cases
applies, so the \textit{else if} in line 5 could be replaced by
\textit{otherwise}. As usual, with a distance value \(\EDcol{j}(i)\)
we can store which of the incoming edges is minimizing, as to remove the
dependency on other entries in \(\EDcol{j-1}\) and \(\EDcol{j}\).}%
\label{Rtabrecurrence}
\[\Rtab(i,j)=\begin{cases}
\mathit{undefined}&\text{if }0\leq j\leq j_{0}-1\\
i&\text{else if }j=j_{0}\\
\Rtab(i,j-1)&\text{else if }\EDcol{j}(i)=\EDcol{j-1}(i)+\delta(\Insertion{\Subchar{v}{j}})\\
\Rtab(i-1,j-1)&\text{else if }\EDcol{j}(i)=\EDcol{j-1}(i-1)+\delta(\Replacement{\Subchar{u}{i}}{\Subchar{v}{j}})\\
\Rtab(i-1,j)&\text{else if }\EDcol{j}(i)=\EDcol{j}(i-1)+\delta(\Deletion{\Subchar{u}{i}})
\end{cases}
\]
\end{algorithm}
%%%END{frame}

%%%BEGIN{frame}{algorithm}
\begin{algorithm}
\caption{A recursive function to compute crosspoints.}
\label{LinearDivideConquer}
\noindent\begin{tabular}{@{}l@{~}l}
\textbf{Input}:
&\(i\), \(j\), \(p\), \(q\) with \(1\leq i\leq m\), \(1\leq j\leq n\),
\(0\leq p\leq m-i+1\),\\
&\(0\leq q\leq n-j+1\), and uninitialized \((m+1)\)-element vectors \(E\)\\
&and \(R\) to store distance and row index columns\\
\textbf{Output}:&table \(\Crosspoints\) of crosspoints
\end{tabular}
%
\medskip
%
\begin{algorithmic}[1]
\Function{\EvalCPname}{$C,i,j,p,q$}%ignore
\If{\(q\geq 2\)}
  \State \(\Assign{j_{0}}{\Rounddown{q/2}}\)
  \State
\(\Evalallcols{j_{0}}{\Substring{u}{i}{i+p-1}}{\Substring{v}{j}{j+q-1}}\)
  \State \(\Assign{i_{0}}{R[p]}\)\Comment{last value of last \(\Rtab\)-column}
  \State \(\Assign{\Crosspoints(j-1+j_{0})}{i-1+i_{0}}\)\Comment{\(i\)/\(j\) refer to strings \(\Rightarrow\) subtract 1}
  \State \(\EvalCP{i}{j}{i_{0}}{j_{0}}\)\Comment{eval.\ upper/left part}
  \State \(\EvalCP{i+i_{0}}{j+j_{0}}{p-i_{0}}{q-j_{0}}\)\Comment{low./right}
\EndIf
\EndFunction
\State create table \(\Crosspoints\) of \(n+1\) entries and set
\(\Assign{\Crosspoints(0)}{0}\) and \(\Assign{\Crosspoints(n)}{m}\)
\State \(\EvalCP{1}{1}{m}{n}\)
%\State derive alignment from \(\Crosspoints\).
\end{algorithmic}
\end{algorithm}
%%%END{frame}
\end{document}
